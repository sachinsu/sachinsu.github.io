baseurl = "https://sachinsu.github.io"
title = "Learnings in IT"
languageCode = "en-us"
theme = "mainroad"

[services]
  [services.googleAnalytics]
    ID = 'UA-169012216-1'  

# [Author] # Used in authorbox
#  name = "Sachin Sunkle"
#  bio = "A Coder in IT"

[Params]
  description = "A technical blog " # Site description. Used in meta description
  copyright = "Sachin Sunkle" # Footer copyright holder, otherwise will use site title
  opengraph = true # Enable OpenGraph if true
  schema = true # Enable Schema
  twitter_cards = true # Enable Twitter Cards if true
  readmore = false # Show "Read more" button in list if true
  authorbox = false # Show authorbox at bottom of pages if true
  toc = true # Enable Table of Contents
  pager = true # Show pager navigation (prev/next links) at the bottom of pages if true
  post_meta = ["author", "date"] # Order of post meta information
  mainSections = ["posts"] # Specify section pages to show on home page and the "Recent articles" widget
  dateformat = "Jan 2 2006" # Change the format of dates
  googleFontsLink = "https://fonts.googleapis.com/css?family=Open+Sans:400,400i,700" # Load Google Fonts
  highlightColor = "" # Deprecated in favor of .Site.Params.style.vars.highlightColor
  author.name = "Sachin Sunkle"
 
[Params.style.vars]
  highlightColor = "#ff9933" # Override highlight color

  # Override font-family sets. Secondary font-family set responsible for pre, code, kbd, and samp tags font
  # Take care of different quotes OR escaping symbols in these params if necessary
  fontFamilyPrimary = "Optima, Candara, 'Noto Sans', source-sans-pro, sans-serif"
  fontFamilySecondary = "segoe-ui, 'Open Sans', Helvetica, Arial, sans-serif"
  # fontFamilySecondary = "SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace"

[Params.logo]
  title = "" # Logo title, otherwise will use site title
  subtitle = "A Simple Technical Blog" # Logo subtitle

[Params.sidebar]
  home = "right" # Configure layout for home page
  list = "left"  # Configure layout for list pages
  single = false # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["social","recent", "taglist"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget

[Params.widgets.social]
  # Enable parts of social widget
  twitter = "SachinS3012"
  linkedin = "sachinsunkle"
  github = "sachinsu"


[languages]
    [languages.en]
        languagename = "English"

        [languages.en.menu]

            [[languages.en.menu.main]]
            name = "About"
            weight = 1
            url = "/about/"

            [[languages.en.menu.main]]
            name = "Blog"
            weight = 2
            url = "/posts/"

            [[languages.en.menu.main]]
            name = "Projects"
            weight = 3
            url = "/projects/"

            [[languages.en.menu.main]]
            name = "Gists"
            weight = 4
            url = "https://gist.github.com/sachinsu"

            [[languages.en.menu.main]]
            name = "Useful Links"
            weight = 5
            url = "/links/home"